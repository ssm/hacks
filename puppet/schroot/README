Two scripts to help testing puppet manifests.

Run "my-puppet-master /path/to/some/site.pp" to start a master with sqlite3
storeconfigs.

Run "my-puppet-agent" in a schroot session, to test puppet manifests through
the master.
